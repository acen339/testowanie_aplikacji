# testowanie_aplikacji
    Skrypt liczący największy wspólny dzielnik.

    Aby użyć skryptu wystarczy uruchomić go w środowisku python np. przez idle 
    lub w konsoli.

    Test aplikacji:
    3 testy jednostkowe,
    -dla dowolnych liczb
    -dla liczb pierwszych
    -dla dwóch tych samych liczb
